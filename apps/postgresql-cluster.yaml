apiVersion: postgresql.cnpg.io/v1
kind: Cluster
metadata:
  name: postgresql
spec:
  instances: 1

  storage:
    size: 10Gi

  superuserSecret:
    name: postgresql-superuser

  postgresql:
    parameters:
      archive_timeout: "3600"
